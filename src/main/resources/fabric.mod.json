{
  "schemaVersion": 1,
  "id": "lockmod",
  "version": "${version}",

  "name": "Lock Mod",
  "description": "Adds locks which allow restriction to use of items, as well as keys to allow their use.",
  "authors": [
    "Qaalum"
  ],

  "license": "MIT License",
  "icon": "assets/lockmod/icon.png",

  "environment": "*",
  "entrypoints": {
    "main": [
      "net.Qaalum.lockmod.LockMod"
    ],
    "client": [
      "net.Qaalum.lockmod.LockModClient"
    ]
  },
  "mixins": [
    "lockmod.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=0.14.11",
    "fabric-api": "*",
    "minecraft": "~1.19.3",
    "java": ">=17"
  }
}
